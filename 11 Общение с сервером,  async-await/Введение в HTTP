Действия браузера перед получением данных:

1. Получени IP адреса по домену (DNS lookup)
2. Отправка запроса серверу (request)
3. Обработка ответа (responce)

-------------------

Состав запроса:
- URI (унифицированный индикатор ресурса), все, что идет после домена
- Method (метод) действие над ресурсом GET, POST, PUT ...
- Host (заголовок, доменное имя)
- Headers (заголовки запроса), в частности gost с доменным именем
- Body (тело запроса) - произвольное содержимое запроса

На примере:

Вы идете на собеседование с намерением (Method) в бизнес - центр (Домен), 
в конкретный кабинет (URI). Принесли резюме (Body)? плюс на входе показали охранникам паспорт 
(Headers)

--------------------

Состав ответа:
- Status (статус) - код из трех цифр, содержит отчет об успешности. 200 - ОК, 404 - NotFound ...
- Headers (доп. информация), как у запроса
- Body (тело ответа) - произвольное содержимое, html-код или объект

ПО этому браузер строит отображение сайта

Браузер - клинет для работы с сайтами, расположенными на серверах в сети
