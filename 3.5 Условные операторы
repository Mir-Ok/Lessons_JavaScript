IF-ELSE (когда немного условий)

if (condition) {
    // код, выполнящийся при соблюдении условия
}

------------------------------------------------

if (condition) {
    // код, выполнящийся при соблюдении условия
} else {
    // код, выполнящийся в противном случае
}

-----------------------------------------------

if (condition) {
    // при первом условии
} else if {
    // при втором условии
} else if {
    // при третьем условии
} else {
    // иначе
} 

-----------------------------------------------
 
 
 Оператор SWITCH 
 
 Подходит, когда хотим сравнить значение с чем-то конкретным
 
 let fruit = 'Яблоко';
 
 switch (fruit) {
    case 'Яблоко':
      console.log('Перед нами яблоко')
      break;
    case 'Банан':
      console.log('Перед нами банан')
      break;  
    case 'Арбуз':
    case 'Вишня':
    case 'Клубника':
      console.log('Это ягода')
      break; 
    default:
      console.log('Такого не знаю')
      break;       
 }
 
 Внутри свитча сть несколько кейсов, и если переменная равна, то выполняем действие. 
 Обязательно брейк, иначе пойдет дальше выполняться.
 
