Автоматом можно перевести в простые типы

1. Number переводит в число. Имеет смысл в число преобразовывать только строки

- Строки легко, но плавающие с точкой, не запятая
- Текст вернет NaN
- Булево вернет 1/0
- null вернет 0
- undefined вернет NaN
- объекты, списки, функции - NaN 

2. ParseInt('42'); // 42 округлит 
3. ParseFloat('3.14') // 3.14

4. String(true);      // 'true'
   String(42);        // '42'
   String(undefined); // 'undefined'
   String(null);      // 'null'
   String({});        // '[object Object]' для объектов у которых нет метода toString(), это недоработка
   String({
    toString() {
      return 'Вот что будет если ';
    }});              // 'Вот что будет если ' потому что определили метод toString() вручную 
   String('text');    // 'text'
   
5. Преобразование в строку массивов.
    Сначала каждый элемент преобразуется по правилам выше, а потом все склеятся в строку через запятую
